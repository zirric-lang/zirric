mod taskcall

import tasks

@tasks.Name("build")
@tasks.Call(fn(opts: BuildTask) {
  return opts.target
})
data BuildTask {
  @tasks.Flag()
  dry: Bool

  @tasks.Flag()
  target: String

  @tasks.Flag()
  count: Int
}
